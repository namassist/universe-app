# Dependencies & lockfiles
node_modules
bun.lock

# Build output
.next
out
build
dist
.turbo

# Generated — do not reformat
apps/api/drizzle
next-env.d.ts
*.tsbuildinfo

# Vendored agent skills — owned upstream, and reformatting breaks their hash
.claude

# Personal global-workflow files at the root only (leading slash = repo root).
# Per-app apps/*/AGENTS.md are project files and stay formatted.
/AGENTS.md
/CLAUDE.md
