import { MenuPage } from "@/components/menus/registry";

export const metadata = { title: "Monitoring Fingerprint" };

export default function Page() {
  return <MenuPage slug="monitoring-fingerprint" mode="manage" />;
}
