import { MenuPage } from "@/components/menus/registry";

export const metadata = { title: "Fleet Allocation" };

export default function Page() {
  return <MenuPage slug="fleet-allocation" mode="view" />;
}
